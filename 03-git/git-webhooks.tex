\documentclass[12pt,a4paper]{article}
\usepackage{graphicx}
\usepackage{float}
\usepackage{svg}

% Подключение общего шаблона для лабораторных работ
\input{../lab-common}

% Настройка переменных для данной лабораторной работы
\renewcommand{\labtitle}{Лабораторная работа №1}
\renewcommand{\labsubtitle}{GitHub webhooks}

\begin{document}

% Титульная страница
\makelabcover

% Общие инструкции
\commoninstructions

\section{Цель работы}

Научиться использовать GitHub webhooks для решения задач автоматизации сборки приложения и его развертывания.

\section{Содержание работы}
%@startuml
%actor Разработчик
%
%rectangle "GitHub Репозиторий" as GitHub
%rectangle "Реверс-прокси" as Proxy
%rectangle "Обработчик\n(виртуальная машина)" as Handler
%rectangle "Приложение\n(виртуальная машина)" as App
%
%Разработчик --> GitHub : Событие
%
%GitHub --> Proxy : Вызов webhook
%Proxy --> Handler : Форвард webhook
%
%Handler --> App : Собрать/развернуть
%Handler ..> GitHub : Статус деплоя
%@enduml
\begin{figure}[H]
\centering
\includesvg{webhooks-schema}
\caption{Схема работы GitHub webhooks}
\label{fig:webhooks-schema}
\end{figure}

В данной работе мы изучим механизм GitHub webhooks — автоматических HTTP запросов, которые отправляются при определенных событиях в репозитории.
Как показано на схеме выше, процесс работы состоит из следующих этапов:
\begin{enumerate}
    \item \textbf{Событие в репозитории}: разработчик выполняет push, создает pull request или другое действие
    \item \textbf{Активация webhook}: GitHub автоматически отправляет HTTP POST-запрос на настроенный URL
    \item \textbf{Обработка через прокси}: реверс-прокси перенаправляет запрос к обработчику
    \item \textbf{Автоматическое развертывание}: обработчик получает webhook и запускает процесс сборки/развертывания приложения
    \item \textbf{Обратная связь}: система может отправить статус выполнения обратно в GitHub
\end{enumerate}

Этот подход позволяет использовать Git-репозиторий не только как средство совместной работы с кодом, но и как центральный
элемент автоматизации процессов разработки и развертывания ПО, что является ключевой практикой DevOps.

\begin{task}{1}
    Настроить виртуальную машину с Ubuntu 24.
\end{task}

\begin{task}{2}
    На виртуальной машине настроить доступ до прокси сервера frp.

    \begin{lstlisting}[language=bash, numbers=none, frame=single, basicstyle=\ttfamily\footnotesize, columns=fullflexible, keepspaces=true, breaklines=false, commentstyle=\color{gray}\itshape, extendedchars=true, texcl=true, showstringspaces=false]
# Настройка переменных. Актуальные значения узнайте у преподавателя!
PROXY=course.prafdin.ru
TOKEN=pravdin2001
ID=prafdin

# Скачать и установить frp как systemd сервисы.
# Команда запросит пароль текущего пользователя
wget -qO- https://gist.github.com/lawrenceching/\
41244a182307940cc15b45e3c4997346/raw/\
0576ea85d898c965c3137f7c38f9815e1233e0d1/\
install-frp-as-systemd-service.sh | sudo bash

# Настройка доступа до frp server.
# Команда запросит пароль текущего пользователя
sudo tee /etc/frp/frpc.toml > /dev/null <<EOF # Начало команды
serverAddr = "$PROXY"
serverPort = 7000

auth.method = "token"
auth.token = "$TOKEN"

[[proxies]]
name = "hook-$ID"
type = "http"
localPort = 8080
customDomains = ["webhook.$ID.$PROXY"]

[[proxies]]
name = "app-$ID"
type = "http"
localPort = 8181
customDomains = ["app.$ID.$PROXY"]
EOF # Конец команды

# Запустите frp client. Команда запросит пароль текущего пользователя
sudo systemctl status frpc

# Проверьте корректность настройки
echo "Адрес для проверки: webhook.$ID.$PROXY"

wget -qO- https://gist.githubusercontent.com/\
prafdin/b9ff40c8bc6dc8c55ca7ac911e278ecc\
/raw/ea80e83e6a36220e2e9ccdaca8fde49ee888f2ab\
/main.py | python3

# Перейдите по адресу для проверки в браузере.
# Если всё настроено правильно, то в браузере получите ok
\end{lstlisting}

\end{task}

\begin{task}{3}
    \begin{enumerate}
        \item \textbf{Easy}: задеплоить статический сайт с помощью nginx. Событие - любой коммит в main ветку.
        \item \textbf{Medium}: задеплоить новую версию python web приложения. Событие - создание любого тега. Выставить статус деплоя на коммит тега.
        \item \textbf{Hard}: собрать Java/C Sharp/C++ web приложение, задеплоить новую версию приложения. Событие - создание GitHub Release. Выставить статус деплоя на коммит релиза.
    \end{enumerate}
    Все необходимые зависимости приложения предварительно могут быть установлены вручную на виртуальной машине.

    При необходимости используйте шаблон python модуля для запуска обработчика webhook https://github.com/prafdin/SampleAppUpdateModule.git

    В качестве приложений можете создать форк следующих проектов:
    \begin{enumerate}
        \item Статический сайт https://github.com/cloudacademy/static-website-example.git
        \item Python приложение TBD
        \item Java приложение TBD
    \end{enumerate}

\end{task}

\section{Контрольные вопросы}
\begin{enumerate}
    \item TBD
\end{enumerate}

\section{Выводы}
TBD
\end{document}