baseURL = "https://prafdin.github.io/devops-course/"
languageCode = "ru"
title = "Курс DevOps"

# Item pages (lectures/assignments/extras) intentionally have no
# layouts/_default/single.html — there's no per-item HTML page, only
# PDF links via the year pages. Disabling taxonomy/sitemap/RSS avoids
# advertising non-existent URLs (no taxonomies are used any more).
disableKinds = ["taxonomy", "term", "sitemap", "RSS"]
